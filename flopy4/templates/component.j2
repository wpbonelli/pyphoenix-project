# Autogenerated file, do not edit!

class {{ component.title() }}{{ subcomponent.title() }}(flopy4.{{ base_cls }}):
    {% for p in params %}
    {{ p.name }} = flopy4.{{ p.cls }}(
        {% for f in p.fields %}{{ f.name }}={{ p[f.name] }},{% endfor %}
    )
    {% endfor %}

    def __init__(
        self,
        {% for p in params %}{{ p.name }},{% endfor %}
    ):
        {% for p in params %}self.{{ p.name }} = {{ p.name }}{% endfor %}